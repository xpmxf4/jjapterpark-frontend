import { atom } from "recoil";

// // 선택된 날짜를 위한 atom 정의
// export const selectedDateState = atom({
//     key: "selectedDateState", // 고유한 ID (다른 atom과 구별하기 위해 필요)
//     default: new Date(localStorage.getItem("selectedDate") || new Date()), // 기본값 설정
// });
